#ifndef MaxPayoff_h
#define MaxPayoff_h

#include<iostream>

double maxPayoff(const double x, double strike);

//double maxPayoff1st(const State& x, double strike)
//{
//	return std::max<double>(x(0) - strike, 0.0);
//}

#endif